# CoBook — PROJECT STATE

## Источник истины

`main` — текущая рабочая и контрольная точка проекта.

## КРИТИЧЕСКИЕ ПРАВИЛА КЛИЕНТА

- Технический `id` — только системный идентификатор сущности. Он скрыт, не показывается пользователю и никогда не используется как код клиента.
- Пользовательский идентификатор называется **код клиента**; в интерфейсе достаточно короткого **«Код»**.
- Если пользователь говорит `клиентский ID`, `ID клиента`, `client ID` и аналогичные формулировки в контексте клиента, он имеет в виду **код клиента**, а не технический `id`.
- Код клиента необязательный. Он состоит ровно из 4 символов: цифры и/или латинские буквы. Число `1` нормализуется в `0001`.
- После первого присвоения код навсегда считается использованным. Удаление кода из текущей карточки не освобождает его для повторного использования.
- История клиента никогда не удаляется и не переносится из-за изменения или удаления кода.
- Telegram ID / Telegram-аккаунт — отдельная техническая сущность связи. Telegram ID не равен клиенту и не является носителем истории клиента.
- У одного клиента может быть несколько Telegram-аккаунтов и несколько телефонов.
- Отвязка Telegram-аккаунта меняет только связь. История прежнего клиента остаётся у прежнего клиента; при последующей привязке того же аккаунта к другой сущности начинается отдельная история нового клиента.
- Для клиентской сущности не использовать термин «профиль». `PROFILE` — профиль мастера и отдельный MODULE.

## ОБЯЗАТЕЛЬНАЯ АРХИТЕКТУРА MODULE

CoBook строится как Mini App из самостоятельных MODULE. Каждый самостоятельный экран/раздел имеет собственную папку-владельца.

Целевая структура:

```text
CoBook/
├── app.js
└── app/
    ├── journal/
    ├── timetable/
    ├── maine/
    │   └── clients/
    ├── chat/
    ├── shared/
    └── settings/
        ├── settings.js
        ├── profile/
        ├── service/
        ├── documents/
        └── loyalty/
```

### КРИТИЧЕСКОЕ ПРАВИЛО ВЛАДЕНИЯ

`Clients` НЕ является самостоятельным MODULE верхнего уровня и НЕ является соседом `maine`.

Клиенты — функциональность `MAINE`, поэтому физически находятся только здесь:

```text
app/maine/clients/
```

Запрещено создавать или возвращать `app/clients/` и регистрировать `clients` как самостоятельный верхнеуровневый маршрут через `CoBook.modules.clients`.

Переход:

```text
Нижнее меню → Главная → Клиенты → список → карточка клиента
```

`state.page` для Клиентов остаётся `maine`. Вложенное состояние хранится внутри `maine`.

## app.js

`app.js` — только запуск приложения. Он не является владельцем функционала разделов.

## app/shared/core.js

`app/shared/core.js` содержит только общий технический механизм: состояние приложения, верхнеуровневую навигацию, маршрутизацию нижнего меню, общий `render`, обработку общих действий и общие утилиты.

`shared` не является владельцем бизнес-функциональности. В `shared` нельзя переносить код конкретного MODULE только ради удобства.

## MODULE ownership

Каждый верхнеуровневый MODULE является единственным владельцем своего экрана, действий, модальных окон, внутренней навигации, данных и локальных стилей. Вложенная функциональность хранится внутри папки владельца и не становится самостоятельным верхнеуровневым MODULE только из-за наличия собственных файлов.

Запрещены:
- `*-router-fix.js`;
- `*-navigation-fix.js`;
- `*-hours-fix.js`;
- `*-ui-fix.js`;
- DOM-перехваты, созданные только для обхода архитектурной ошибки;
- дублирование одного `render`/экрана в нескольких местах;
- временные `window.*`-мосты между MODULE;
- реализация одного раздела одновременно в `app.js` и отдельном MODULE;
- создание `app/clients/` для `MAINE → Клиенты`;
- регистрация `Clients` как `CoBook.modules.clients`.

Если проблема возникает в навигации, исправляется центральная маршрутизация или владелец MODULE, а не создаётся обход.

## Текущие границы

- `maine` — Главная мастера и владелец функциональности `Клиенты`.
- `maine/clients` — внутренняя реализация `MAINE → Клиенты`; не является верхнеуровневым MODULE.
- `SERVICE`, `PROFILE`, `DOCUMENTS`, `LOYALTY` — самостоятельные MODULE внутри `SETTINGS`.
- `JOURNAL`, `TIMETABLE`, `MAINE`, `CHAT`, `SETTINGS` — MODULE нижней навигации.
- `SERVICE`, `PROFILE`, `DOCUMENTS`, `LOYALTY` не управляют другими MODULE.

## Хранилища

- `cobook_clients` — текущие данные сущностей клиентов.
- `cobook_client_codes` — глобальный реестр навсегда использованных кодов клиента.
- `cobook_telegram_accounts` — Telegram-аккаунты, их текущие связи и история связей.
- `cobook_loyalty_programs`
- `cobook_loyalty_accounts`
- `cobook_deposit_programs`
- `cobook_loyalty_deposits`
- `cobook_referrals`
- `cobook_certificates`
- `cobook_subscriptions`
- `cobook_profile`

## ОБЯЗАТЕЛЬНЫЙ ПРОТОКОЛ НАЧАЛА КАЖДОЙ РАБОЧЕЙ ЗАДАЧИ

Перед выполнением **каждой рабочей задачи** ChatGPT обязан заново прочитать актуальный `PROJECT_STATE.md` из `main` и использовать его как обязательный источник правил для этой задачи.

Рабочей задачей считается любое обращение пользователя, которое требует изменения, исправления, проверки, реализации, рефакторинга, публикации или иного действия над проектом, включая `теперь работаем`, `сделай`, `добавь`, `исправь`, `проверь`, `реализуй` и аналогичные формулировки.

Исключение: если пользователь явно переводит разговор в режим обсуждения (`давай обсудим`, `теперь вопрос`, `давай поговорим`, обсуждение архитектуры без просьбы о реализации), чтение `PROJECT_STATE.md` перед ответом не требуется. Как только обсуждение переходит в реализацию — `PROJECT_STATE.md` снова читается до начала работы.

После чтения документа обязательны:
1. определить применимые правила и границы;
2. проверить существующую архитектуру и текущую реализацию до изменения;
3. реализовать внутри существующего паттерна и владельца MODULE;
4. проверить отсутствие дубликатов обработчиков, маршрутов, `render`, CSS и других параллельных реализаций;
5. проверить все затронутые сценарии;
6. проверить отсутствие регрессий;
7. пройти полный протокол завершения;
8. только после успешного прохождения всех проверок формировать commit и ссылку на опубликованное приложение.

## ОБЯЗАТЕЛЬНЫЙ ПРОТОКОЛ ЗАВЕРШЕНИЯ ЗАДАЧИ

1. Проверить синтаксис всех JavaScript-файлов проекта.
2. Проверить загрузку всех подключаемых скриптов и отсутствие ошибок JavaScript.
3. Проверить основные маршруты нижнего меню.
4. Проверить переходы `SETTINGS → PROFILE / SERVICE / DOCUMENTS / LOYALTY` и возврат.
5. Проверить основные действия изменённого MODULE.
6. Проверить отсутствие регрессий в связанных MODULE.
7. Проверить GitHub Pages deploy для того же commit SHA.
8. Проверить фактическую опубликованную версию, а не только файлы GitHub.
9. Для интерактивных изменений проверить реальный сценарий: нажатие → изменение состояния → render → следующий экран → возврат → повторное открытие.
10. Проверить отсутствие дублированных обработчиков, маршрутов, DOM-перехватов и CSS-override обходного характера.
11. Только после этого разрешены commit и ссылка пользователю.

Непроверенное изменение не считается выполненной задачей.
