# CoBook — PROJECT STATE

## Источник истины

`main` — текущая рабочая и контрольная точка проекта.

## ОБЯЗАТЕЛЬНАЯ АРХИТЕКТУРА MODULE

CoBook строится как Mini App из самостоятельных MODULE. Каждый самостоятельный экран/раздел имеет собственную папку-владельца.

Целевая структура:

```text
CoBook/
├── app.js                         # только запуск и центральная маршрутизация
└── app/
    ├── journal/                   # нижнее меню → Журнал
    ├── timetable/                # нижнее меню → График
    ├── maine/                    # нижнее меню → Главная
    ├── clients/                  # Главная → Клиенты
    ├── chat/                     # нижнее меню → Чат
    └── settings/                 # нижнее меню → Настройки
        ├── settings.js
        ├── service/              # Настройки → Сервис
        ├── documents/            # Настройки → Документы
        └── loyalty/              # Настройки → Лояльность
```

Для каждого MODULE допускаются собственные `*.js`, `*.css` и внутренние файлы. Общие технические элементы находятся только в `app/shared/`.

### app.js

`app.js` — только запуск и центральная маршрутизация. Он не является владельцем функционала любого раздела и не содержит реализацию экранов MODULE.

### MODULE ownership

Каждый MODULE является единственным владельцем своего экрана, действий, модальных окон, внутренней навигации, данных и локальных стилей. Другой MODULE не изменяет его внутреннюю реализацию.

Запрещены:
- `*-router-fix.js`;
- `*-navigation-fix.js`;
- `*-hours-fix.js`;
- `*-ui-fix.js`;
- DOM-перехваты, созданные только для обхода архитектурной ошибки;
- дублирование одного `render`/экрана в нескольких местах;
- временные `window.*`-мосты между MODULE;
- реализация одного раздела одновременно в `app.js` и отдельном MODULE.

Если проблема возникает в навигации, исправляется центральная маршрутизация или владелец MODULE, а не создаётся обход.

## Текущие границы

- `maine` — Главная мастера.
- `clients` — отдельный MODULE, открываемый из Главной.
- `SERVICE` — самостоятельный MODULE внутри `SETTINGS`: `SETTINGS → SERVICE → PROCEDURES / PRODUCTS`.
- `DOCUMENTS` — самостоятельный MODULE внутри `SETTINGS`.
- `LOYALTY` — самостоятельный MODULE внутри `SETTINGS`.
- `JOURNAL`, `TIMETABLE`, `MAINE`, `CHAT`, `SETTINGS` — MODULE нижней навигации.
- `SERVICE`, `DOCUMENTS`, `LOYALTY` не управляют другими MODULE.

## Клиенты и идентификаторы

В интерфейсе рабочая сущность мастера называется **Клиент**.

`client_id` — единственный якорь клиента и всегда ровно четырёхзначное число `0000–9999`.

`telegram_id` — идентификатор конкретного Telegram-аккаунта. Он не является `client_id` и автоматически не создаёт нового клиента.

Один `client_id` может иметь несколько связанных Telegram ID. Связь выполняет мастер.

Не вводить альтернативные идентификаторы (`profile_id`, `client_001` и т.п.) без прямого решения пользователя.

## LOYALTY

`SETTINGS → LOYALTY` содержит независимые механизмы:
- `Программы` — условия и награды: скидка %, бесплатно (процедура или ручной подарок), деньги;
- `Сертификаты` — отдельный предоплаченный номинал и баланс;
- `Абонементы` — пакет предоплаченных посещений/услуг;
- `Реферальная программа` — денежное или процентное вознаграждение;
- `Личный счёт` — денежный баланс с начислениями и списаниями;
- `Депозиты` — предоплаченная программа с фиксированным взносом, скидкой и периодом действия.

### Навигация LOYALTY

Во всех подразделах и редакторах `LOYALTY` используется один общий элемент `section-back-button`.

- текст кнопки всегда `Назад`;
- кнопка находится в едином нижнем контейнере `section-back-wrap` непосредственно перед основной нижней навигацией приложения;
- из подраздела кнопка возвращает на главный экран `LOYALTY`;
- из главного экрана `LOYALTY` тот же элемент возвращает в `SETTINGS`;
- не создавать отдельные `loyalty-navigation`, `loyalty-back` или DOM-фиксы для отдельных экранов.

## Личный счёт

Личный счёт хранится отдельно от депозита. Баланс изменяется операциями `Начисление` и `Списание`. Недостаточный баланс не допускает списание. История операций хранится вместе со счётом.

## Депозит

Депозитная программа содержит название, фиксированный первоначальный взнос, процент скидки, дату начала и дату завершения.

При подключении программы клиенту создаётся отдельный депозит с исходным балансом, равным взносу программы. У депозита сохраняются клиент, программа, баланс, скидка и период действия.

Баланс уменьшается операциями списания. Недостаточный баланс не допускает списание. После даты завершения неиспользованный остаток считается сгоревшим. Депозит не пополняется автоматически и не смешивается с `Личным счётом`.

## Хранилища

- `cobook_loyalty_programs`
- `cobook_loyalty_accounts`
- `cobook_deposit_programs`
- `cobook_loyalty_deposits`
- `cobook_referrals`
- `cobook_certificates`
- `cobook_subscriptions`

## Проверка

Изменение считается рабочим только после проверки синтаксиса всех JS, загрузки Mini App, каждого маршрута нижнего меню, каждого перехода `SETTINGS → SERVICE / DOCUMENTS / LOYALTY`, возврата из каждого подраздела, основных действий изменённого MODULE, отсутствия регрессий и успешного GitHub Pages deploy для того же commit SHA.

Визуальный рендер без работающего действия не считается реализацией.

## Правила разработки

1. Работать непосредственно в `main`.
2. Перед изменением читать этот `PROJECT_STATE.md` из текущего `main`.
3. Не менять архитектурные границы без решения пользователя.
4. Не выдавать визуальный рендер за реализованный функционал.
5. Не объявлять изменение рабочим, пока код и маршрут не проверены.
6. Не создавать обходные фиксы вместо исправления владельца MODULE или центрального маршрута.
7. Изменение одного MODULE не должно менять другой MODULE без явной необходимости.
8. Перед коммитом проверять приложение целиком.
9. В ответе по завершённой задаче указывать полный SHA коммита и рабочую ссылку на Mini App.
