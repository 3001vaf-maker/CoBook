(function(){
  window.CoBook=window.CoBook||{};
  CoBook.modules=CoBook.modules||{};
  function esc(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;')}
  function normalize(p){if(!p.priceType){if(p.priceFrom||p.priceTo)p.priceType=p.priceTo?'range':'from';else if(p.price)p.priceType='exact';else p.priceType='exact'}if(p.priceFrom==null&&p.price)p.priceFrom=p.price;if(p.priceTo==null)p.priceTo='';return p}
  function workplaces(){try{const p=JSON.parse(localStorage.getItem('cobook_profile')||'{}');return Array.isArray(p.workplaces)?p.workplaces.map((w,i)=>({...w,id:w.id||`workplace-${i}`,name:w.name||`Место работы ${i+1}`})):[]}catch(e){return []}}
  function priceVariant(v={}){const type=v.priceType||'exact';return {workplaceId:v.workplaceId||'',priceType:type,priceFrom:v.priceFrom??v.price??'',priceTo:v.priceTo??''}}
  function procedureVariants(item){if(Array.isArray(item.prices)&&item.prices.length)return item.prices.map(priceVariant);return [priceVariant({workplaceId:item.workplaceId||'',priceType:item.priceType,priceFrom:item.priceFrom,priceTo:item.priceTo,price:item.price})]}
  function render(){const procedures=state.serviceMode==='procedures',views=CoBook.serviceViews||{},view=procedures?(views.procedures?.render?views.procedures.render(state):''):(views.products?.render?views.products.render(state):'');return shell(`<section class="page-head service-head"><div class="eyebrow">НАСТРОЙКИ · СЕРВИС</div><h1>Сервис</h1></section><div class="service-tabs" role="tablist"><button class="service-tab ${procedures?'active':''}" data-action="service-tab" data-mode="procedures" type="button" role="tab" aria-selected="${procedures}">Процедуры</button><button class="service-tab ${!procedures?'active':''}" data-action="service-tab" data-mode="products" type="button" role="tab" aria-selected="${!procedures}">Товары</button></div><div class="service-view">${view}</div><button class="section-back-button" data-action="navigate" data-page="settings" type="button">Назад</button>`)}
  function workplaceOptions(selected){const ws=workplaces();return `<option value="" ${!selected?'selected':''}>Без привязки к месту</option>${ws.map(w=>`<option value="${esc(w.id)}" ${selected===w.id?'selected':''}>${esc(w.name)}</option>`).join('')}`}
  function priceBlock(v,i){const type=v.priceType||'exact';return `<div class="procedure-price-variant" data-price-variant data-variant-index="${i}"><label class="service-field"><span>Место работы</span><select data-field="workplace">${workplaceOptions(v.workplaceId)}</select></label><div class="price-mode-label">Цена</div><div class="price-mode" data-price-mode><button type="button" class="price-mode-btn ${type==='exact'?'active':''}" data-action="price-type" data-price-type="exact">Цена</button><button type="button" class="price-mode-btn ${type==='from'?'active':''}" data-action="price-type" data-price-type="from">От</button><button type="button" class="price-mode-btn ${type==='range'?'active':''}" data-action="price-type" data-price-type="range">От–до</button></div><div class="price-fields ${type==='range'?'range':''}"><label class="service-field"><span>${type==='exact'?'Цена':'Цена от'}</span><input data-field="price-from" inputmode="decimal" value="${esc(v.priceFrom||'')}"></label><label class="service-field price-to-field" ${type==='range'?'':'hidden'}><span>Цена до</span><input data-field="price-to" inputmode="decimal" value="${esc(v.priceTo||'')}"></label></div>${i>0?'<button type="button" class="danger full procedure-price-remove" data-action="remove-price-variant">Удалить вариант цены</button>':''}</div>`}
  function modal(kind,index=null){const isProcedure=kind==='procedure',source=isProcedure?state.procedures:state.products,item=index===null?(isProcedure?{name:'',duration:60,prices:[priceVariant()]}:{name:'',price:'',image:''}):normalize({...source[index]});if(isProcedure){const variants=procedureVariants(item);return `<div class="service-overlay" data-modal="service" data-kind="procedure" data-index="${index===null?'':index}"><div class="service-sheet procedure-editor"><div class="service-sheet-head"><b>${index===null?'Новая процедура':'Процедура'}</b><button class="service-close" data-action="modal-close" type="button">×</button></div><label class="service-field"><span>Наименование процедуры</span><input data-field="name" autocomplete="off" value="${esc(item.name)}"></label><label class="service-field"><span>Время в работе</span><input data-field="duration" type="number" min="1" inputmode="numeric" value="${esc(item.duration)}"></label><div class="procedure-prices" data-procedure-prices>${variants.map(priceBlock).join('')}</div><button type="button" class="secondary full" data-action="add-price-variant">+ Добавить место работы</button><button class="primary full" data-action="save-service" type="button">Сохранить</button><button class="section-back-button" data-action="modal-close" type="button">Назад</button></div></div>`}return `<div class="service-overlay" data-modal="service" data-kind="product" data-index="${index===null?'':index}"><div class="service-sheet product-editor"><div class="service-sheet-head"><b>${index===null?'Новый товар':'Товар'}</b><button class="service-close" data-action="modal-close" type="button">×</button></div><label class="product-upload" data-product-upload><span class="product-upload-preview">${item.image?`<img src="${item.image}" alt="">`:'+'}</span><span><b>Фото товара</b><small>Выбрать изображение</small></span><input type="file" accept="image/*" data-field="image-file"></label><label class="service-field"><span>Название товара</span><input data-field="name" autocomplete="off" value="${esc(item.name)}"></label><label class="service-field"><span>Стоимость</span><input data-field="price" inputmode="decimal" value="${esc(item.price||'')}"></label><button class="primary full" data-action="save-service" type="button">Сохранить</button><button class="section-back-button" data-action="modal-close" type="button">Назад</button></div></div>`}
  function save(modal){const name=modal.querySelector('[data-field="name"]')?.value.trim()||'',kind=modal.dataset.kind,index=modal.dataset.index===''?null:Number(modal.dataset.index);if(!name)return;if(kind==='procedure'){const duration=Math.max(1,Number(modal.querySelector('[data-field="duration"]')?.value)||60),prices=[...modal.querySelectorAll('[data-price-variant]')].map(v=>{const type=v.querySelector('[data-price-type].active')?.dataset.priceType||'exact';return {workplaceId:v.querySelector('[data-field="workplace"]')?.value||'',priceType:type,priceFrom:v.querySelector('[data-field="price-from"]')?.value.trim()||'',priceTo:type==='range'?(v.querySelector('[data-field="price-to"]')?.value.trim()||''):''}}).filter(v=>v.priceFrom||v.priceTo||v.workplaceId);const item={name,duration,prices:prices.length?prices:[priceVariant()]};if(index===null)state.procedures.push(item);else state.procedures[index]=item;localStorage.setItem('cobook_procedures',JSON.stringify(state.procedures))}else{const price=modal.querySelector('[data-field="price"]')?.value.trim()||'',old=index===null?{}:(state.products[index]||{}),file=modal.querySelector('[data-field="image-file"]'),image=file?.dataset.imageData||old.image||'',item={name,price,image};if(index===null)state.products.push(item);else state.products[index]=item;localStorage.setItem('cobook_products',JSON.stringify(state.products))}modal.remove();window.render()}
  function handle(a,e,m){if(a==='service-tab'){state.serviceMode=e.dataset.mode;window.render();return}if(a==='open-procedure'){document.body.insertAdjacentHTML('beforeend',modal('procedure'));return}if(a==='open-product'){document.body.insertAdjacentHTML('beforeend',modal('product'));return}if(a==='edit-procedure'){document.body.insertAdjacentHTML('beforeend',modal('procedure',Number(e.dataset.index)));return}if(a==='edit-product'){document.body.insertAdjacentHTML('beforeend',modal('product',Number(e.dataset.index)));return}if(a==='delete-procedure'){state.procedures.splice(Number(e.dataset.index),1);localStorage.setItem('cobook_procedures',JSON.stringify(state.procedures));window.render();return}if(a==='delete-product'){state.products.splice(Number(e.dataset.index),1);localStorage.setItem('cobook_products',JSON.stringify(state.products));window.render();return}if(a==='add-price-variant'){const wrap=e.closest('[data-modal="service"]')?.querySelector('[data-procedure-prices]');if(!wrap)return;wrap.insertAdjacentHTML('beforeend',priceBlock(priceVariant(),wrap.querySelectorAll('[data-price-variant]').length));return}if(a==='remove-price-variant'){e.closest('[data-price-variant]')?.remove();return}if(a==='price-type'){const b=e,m=b.closest('[data-price-variant]');if(!m)return;m.querySelectorAll('[data-price-type]').forEach(x=>x.classList.remove('active'));b.classList.add('active');const type=b.dataset.priceType,to=m.querySelector('.price-to-field'),from=m.querySelector('[data-field="price-from"]')?.closest('.service-field');if(from)from.querySelector('span').textContent=type==='exact'?'Цена':'Цена от';if(to)to.hidden=type!=='range';const fields=m.querySelector('.price-fields');if(fields)fields.classList.toggle('range',type==='range');return}if(a==='save-service'&&m)return save(m)}
  document.addEventListener('change',e=>{const file=e.target.closest('[data-field="image-file"]');if(!file?.files?.[0])return;const reader=new FileReader();reader.onload=()=>{file.dataset.imageData=reader.result;const p=file.closest('[data-product-upload]')?.querySelector('.product-upload-preview');if(p)p.innerHTML=`<img src="${reader.result}" alt="">`};reader.readAsDataURL(file.files[0])},true);
  CoBook.modules.service={render,handle};
})();
