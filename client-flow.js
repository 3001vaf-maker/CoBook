/* CoBook — клиент: слой → услуга → дата → время → подтверждение → главная */
function bookingLayer(){const key='cobook-booking-layer';const saved=JSON.parse(localStorage.getItem(key)||'null');return saved||{name:'Мастер',options:[{id:'master-1',name:db.master.name,active:true}]};}
function clientLayer(){const layer=bookingLayer();const options=(layer.options||[]).filter(x=>x.active!==false);return shell(`<div class="stepbar"><b>1. ${esc(layer.name)}</b><span>2. Услуга</span><span>3. Дата</span><span>4. Время</span><span>5. Подтверждение</span></div><h2>${esc(layer.name)}</h2><div class="small muted">Выберите ${esc(layer.name.toLowerCase())} для записи.</div><div style="margin-top:12px">${options.map(x=>`<button class="service" data-cflow-layer-option="${esc(x.id)}"><div class="row"><b>${esc(x.name)}</b><span>›</span></div></button>`).join('')||'<div class="empty card">Нет доступных вариантов.</div>'}</div>`,'');}
function clientMonthCalendar(){const days=allMonthDays(st.month),week=['Пн','Вт','Ср','Чт','Пт','Сб','Вс'],s=svc(st.service);return `<div class="month-head"><button class="secondary" data-cflow-month="prev">‹</button><b>${esc(monthName(st.month))}</b><button class="secondary" data-cflow-month="next">›</button></div><div class="month-grid">${week.map(x=>`<div class="weekday">${x}</div>`).join('')}${days.map(x=>{const other=x.slice(0,7)!==st.month;const working=isWorking(x);const available=!!s&&slots(x,s).length>0;const statusClass=working?(available?'working':'working-no-time'):'off';return `<button class="month-day ${other?'other':''} ${statusClass} ${x===st.date?'selected':''}" data-cflow-date="${x}" ${available&&!other?'':'disabled'}><span>${D(x).getDate()}</span></button>`}).join('')}</div><div class="calendar-legend"><span><i>●</i> рабочий день</span><span><i>—</i> выходной</span></div>`;}
function clientBooking(){const s=svc(st.service);if(st.page==='selectLayer')return clientLayer();if(st.page==='selectDate')return shell(`<div class="stepbar"><span>1. ${esc(bookingLayer().name)}</span><span>2. Услуга</span><b>3. Дата</b><span>4. Время</span><span>5. Подтверждение</span></div><div class="row"><div><h2>Выберите дату</h2><div class="small muted">${esc(s.name)} · ${s.duration} мин · ${money(s.price)}</div></div><button class="secondary" data-cflow-back="service">← Услуга</button></div>${clientMonthCalendar()}`,'');if(st.page==='selectTime'){const sl=slots(st.date,s);return shell(`<div class="stepbar"><span>1. ${esc(bookingLayer().name)}</span><span>2. Услуга</span><span>3. Дата</span><b>4. Время</b><span>5. Подтверждение</span></div><div class="row"><div><h2>Выберите время</h2><div class="small muted">${esc(s.name)}</div><div class="small muted">${fmt(st.date)}</div></div><button class="secondary" data-cflow-back="date">← Дата</button></div>${sl.length?`<div class="grid">${sl.map(t=>`<button class="slot" data-cflow-slot="${t}">${t}</button>`).join('')}`:'<div class="empty card">На эту дату свободного времени нет.</div>'}`,'');}if(st.page==='confirm')return confirmPage();return shell(`<div class="stepbar"><span>1. ${esc(bookingLayer().name)}</span><b>2. Услуга</b><span>3. Дата</span><span>4. Время</span><span>5. Подтверждение</span></div><h2>Выберите услугу</h2>${db.services.filter(x=>x.active).map(x=>`<button class="service" data-cflow-service="${x.id}"><div class="row"><div><b>${esc(x.name)}</b><div class="small muted">${x.duration} минут</div></div><b>${money(x.price)}</b></div></button>`).join('')}`,'');}
function clientOverride(){if(st.page==='home')return clientHome();return clientBooking();}
render=function(){document.getElementById('app').innerHTML=st.role?(st.role==='master'?master():clientOverride()):home();bind();};
document.addEventListener('click',e=>{const role=e.target.closest('[data-role]');if(role){e.preventDefault();e.stopImmediatePropagation();st.role=role.dataset.role;st.page='home';st.service=null;st.slot=null;st.date=today();st.month=st.date.slice(0,7);render();return;}if(st.role!=='client')return;const newBooking=e.target.closest('[data-new-booking]');if(newBooking){e.preventDefault();e.stopImmediatePropagation();st.page='selectLayer';st.service=null;st.slot=null;st.date=today();st.month=st.date.slice(0,7);render();return;}const layerOption=e.target.closest('[data-cflow-layer-option]');if(layerOption){e.preventDefault();e.stopImmediatePropagation();st.layerOption=layerOption.dataset.cflowLayerOption;st.page='selectService';st.service=null;st.slot=null;render();return;}const service=e.target.closest('[data-cflow-service]');if(service){e.preventDefault();e.stopImmediatePropagation();st.service=service.dataset.cflowService;st.date=today();st.month=st.date.slice(0,7);st.slot=null;st.page='selectDate';render();return;}const date=e.target.closest('[data-cflow-date]');if(date&&!date.disabled){e.preventDefault();e.stopImmediatePropagation();st.date=date.dataset.cflowDate;st.month=st.date.slice(0,7);st.slot=null;st.page='selectTime';render();return;}const slot=e.target.closest('[data-cflow-slot]');if(slot){e.preventDefault();e.stopImmediatePropagation();st.slot=slot.dataset.cflowSlot;st.page='confirm';render();return;}const back=e.target.closest('[data-cflow-back]');if(back){e.preventDefault();e.stopImmediatePropagation();st.slot=null;st.page=back.dataset.cflowBack==='service'?'selectService':'selectDate';render();return;}const month=e.target.closest('[data-cflow-month]');if(month){e.preventDefault();e.stopImmediatePropagation();const d=D(st.month+'-01');d.setMonth(d.getMonth()+(month.dataset.cflowMonth==='next'?1:-1));st.month=iso(d).slice(0,7);render();return;}},true);
render();
