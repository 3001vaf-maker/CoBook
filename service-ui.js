(function(){
  const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;');
  function normalize(p){if(!p.priceType){if(p.priceFrom||p.priceTo){p.priceType=p.priceTo?'range':'from';}else if(p.price){p.priceType='exact';p.priceFrom=p.price;}else p.priceType='exact';}if(p.priceFrom==null&&p.price)p.priceFrom=p.price;if(p.priceTo==null)p.priceTo='';return p;}
  function priceText(p){normalize(p);if(p.priceType==='range'&&p.priceFrom&&p.priceTo)return `${esc(p.priceFrom)}–${esc(p.priceTo)}`;if(p.priceType==='from'&&p.priceFrom)return `от ${esc(p.priceFrom)}`;if(p.priceFrom)return esc(p.priceFrom);return '—';}
  window.procedureCard=function(p,i){normalize(p);return `<article class="procedure-card"><button class="procedure-card-main" data-action="edit-procedure" data-index="${i}" type="button"><div class="procedure-card-name">${esc(p.name)}</div><div class="procedure-card-detail"><span>${esc(p.duration)} мин</span><span>${priceText(p)}</span></div></button><button class="procedure-delete" data-action="delete-procedure" data-index="${i}" type="button" aria-label="Удалить процедуру">🗑</button></article>`;};
  function productCard(p,i){const image=p.image?`<img src="${p.image}" alt="">`:`<span class="product-image-placeholder">▱</span>`;return `<article class="product-card"><button class="product-card-main" data-action="edit-product" data-index="${i}" type="button"><span class="product-image">${image}</span><span class="product-card-info"><b>${esc(p.name)}</b><small>${p.price?esc(p.price):'Цена не указана'}</small></span></button><button class="product-delete" data-action="delete-product" data-index="${i}" type="button" aria-label="Удалить товар">🗑</button></article>`;}
  window.service=function(){const procedures=state.serviceMode==='procedures';return shell(`<section class="page-head service-head"><div class="eyebrow">НАСТРОЙКИ · СЕРВИС</div><h1>Сервис</h1></section><div class="service-tabs"><button class="service-tab ${procedures?'active':''}" data-action="service-tab" data-mode="procedures" type="button">Процедуры</button><button class="service-tab ${!procedures?'active':''}" data-action="service-tab" data-mode="products" type="button">Товары</button></div>${procedures?`<section class="procedure-list">${state.procedures.map(procedureCard).join('')}</section><button class="primary full service-add" data-action="open-procedure" type="button">Добавить процедуру</button>`:`<section class="product-list">${state.products.map(productCard).join('')}</section><button class="primary full service-add" data-action="open-product" type="button">Добавить товар</button>`}<button class="service-back full" data-action="navigate" data-page="settings" type="button">Назад в Настройки</button>`)};
  window.serviceModal=function(kind,index=null){const isProcedure=kind==='procedure',source=isProcedure?state.procedures:state.products,item=index===null?(isProcedure?{name:'',duration:60,priceType:'exact',priceFrom:'',priceTo:''}:{name:'',price:'',image:''}):normalize({...source[index]});if(isProcedure){const type=item.priceType||'exact';return `<div class="service-overlay" data-modal="service" data-kind="procedure" data-index="${index===null?'':index}"><div class="service-sheet procedure-editor"><div class="service-sheet-head"><b>${index===null?'Новая процедура':'Процедура'}</b><button class="service-close" data-modal-action="close" type="button">×</button></div><label class="service-field"><span>Наименование процедуры</span><input data-field="name" autocomplete="off" value="${esc(item.name)}"></label><label class="service-field"><span>Время в работе</span><input data-field="duration" type="number" min="1" inputmode="numeric" value="${esc(item.duration)}"></label><div class="price-mode-label">Цена</div><div class="price-mode" data-price-mode><button type="button" class="price-mode-btn ${type==='exact'?'active':''}" data-price-type="exact">Цена</button><button type="button" class="price-mode-btn ${type==='from'?'active':''}" data-price-type="from">От</button><button type="button" class="price-mode-btn ${type==='range'?'active':''}" data-price-type="range">От–до</button></div><div class="price-fields ${type==='range'?'range':''}"><label class="service-field"><span>${type==='exact'?'Цена':'Цена от'}</span><input data-field="price-from" inputmode="decimal" value="${esc(item.priceFrom||'')}"></label><label class="service-field price-to-field" ${type==='range'?'':'hidden'}><span>Цена до</span><input data-field="price-to" inputmode="decimal" value="${esc(item.priceTo||'')}"></label></div><button class="primary full" data-modal-action="save-service" type="button">Сохранить</button><button class="service-back full" data-modal-action="close" type="button">Назад в Сервис</button></div></div>`;}return `<div class="service-overlay" data-modal="service" data-kind="product" data-index="${index===null?'':index}"><div class="service-sheet product-editor"><div class="service-sheet-head"><b>${index===null?'Новый товар':'Товар'}</b><button class="service-close" data-modal-action="close" type="button">×</button></div><label class="product-upload" data-product-upload><span class="product-upload-preview">${item.image?`<img src="${item.image}" alt="">`:'+'}</span><span><b>Фото товара</b><small>Выбрать изображение</small></span><input type="file" accept="image/*" data-field="image-file"></label><label class="service-field"><span>Название товара</span><input data-field="name" autocomplete="off" value="${esc(item.name)}"></label><label class="service-field"><span>Стоимость</span><input data-field="price" inputmode="decimal" value="${esc(item.price||'')}"></label><button class="primary full" data-modal-action="save-service" type="button">Сохранить</button><button class="service-back full" data-modal-action="close" type="button">Назад в Сервис</button></div></div>`;};
  window.saveService=function(modal){const name=modal.querySelector('[data-field="name"]')?.value.trim()||'',kind=modal.dataset.kind,index=modal.dataset.index===''?null:Number(modal.dataset.index);if(!name)return;if(kind==='procedure'){const duration=Math.max(1,Number(modal.querySelector('[data-field="duration"]')?.value)||60),type=modal.querySelector('[data-price-type].active')?.dataset.priceType||'exact',from=modal.querySelector('[data-field="price-from"]')?.value.trim()||'',to=modal.querySelector('[data-field="price-to"]')?.value.trim()||'',item={name,duration,priceType:type,priceFrom:from,priceTo:type==='range'?to:''};if(index===null)state.procedures.push(item);else state.procedures[index]=item;localStorage.setItem('cobook_procedures',JSON.stringify(state.procedures));}else{const price=modal.querySelector('[data-field="price"]')?.value.trim()||'',old=index===null?{}:(state.products[index]||{}),file=modal.querySelector('[data-field="image-file"]'),image=file?.dataset.imageData||old.image||'',item={name,price,image};if(index===null)state.products.push(item);else state.products[index]=item;localStorage.setItem('cobook_products',JSON.stringify(state.products));}modal.remove();render();};
  window.management=function(){const count=Array.isArray(window.cobookClients)?window.cobookClients.length:Number(localStorage.getItem('cobook_clients_count')||0);return shell(`<section class="hero"><div class="eyebrow">КАБИНЕТ МАСТЕРА</div><h1>Управление</h1></section><button class="management-folder" data-action="navigate" data-page="clients" type="button"><span class="management-folder-icon">♙</span><span><b>Клиенты</b><small>${count} ${count===1?'клиент':'клиентов'}</small></span><span class="management-chevron">›</span></button>`)};
  window.clients=function(){const count=Array.isArray(window.cobookClients)?window.cobookClients.length:Number(localStorage.getItem('cobook_clients_count')||0);return shell(`<section class="page-head"><div class="eyebrow">УПРАВЛЕНИЕ · КЛИЕНТЫ</div><h1>Клиенты</h1><p>Клиентов: ${count}</p></section>`)};
  window.settings=function(){return shell(`<section class="page-head"><div class="eyebrow">РАБОЧИЕ НАСТРОЙКИ</div><h1>Настройки</h1></section><button class="management-folder" data-action="navigate" data-page="service" type="button"><span class="management-folder-icon">▱</span><span><b>Сервис</b><small>Процедуры и товары</small></span><span class="management-chevron">›</span></button><button class="management-folder" data-action="navigate" data-page="documents" type="button"><span class="management-folder-icon">▤</span><span><b>Документы</b><small>Согласия и документы</small></span><span class="management-chevron">›</span></button>`)};
  window.documents=function(){return shell(`<section class="page-head"><div class="eyebrow">НАСТРОЙКИ · ДОКУМЕНТЫ</div><h1>Документы</h1></section>`)};
  document.body.addEventListener('click',e=>{const mode=e.target.closest('[data-price-type]');if(!mode)return;const modal=mode.closest('[data-modal="service"]');if(!modal)return;modal.querySelectorAll('[data-price-type]').forEach(b=>b.classList.remove('active'));mode.classList.add('active');const type=mode.dataset.priceType,fromLabel=modal.querySelector('[data-field="price-from"]')?.closest('.service-field'),toLabel=modal.querySelector('.price-to-field');if(fromLabel)fromLabel.querySelector('span').textContent=type==='exact'?'Цена':'Цена от';if(toLabel)toLabel.hidden=type!=='range';});
  document.body.addEventListener('change',e=>{const file=e.target.closest('[data-field="image-file"]');if(!file?.files?.[0])return;const reader=new FileReader();reader.onload=()=>{file.dataset.imageData=reader.result;const preview=file.closest('[data-product-upload]')?.querySelector('.product-upload-preview');if(preview)preview.innerHTML=`<img src="${reader.result}" alt="">`;};reader.readAsDataURL(file.files[0]);});
  app.addEventListener('click',e=>{const b=e.target.closest('[data-action="delete-procedure"],[data-action="delete-product"]');if(!b||!app.contains(b))return;const i=Number(b.dataset.index);if(!Number.isInteger(i))return;if(b.dataset.action==='delete-procedure'){if(!state.procedures[i])return;state.procedures.splice(i,1);localStorage.setItem('cobook_procedures',JSON.stringify(state.procedures));}else{if(!state.products[i])return;state.products.splice(i,1);localStorage.setItem('cobook_products',JSON.stringify(state.products));}render();});
})();